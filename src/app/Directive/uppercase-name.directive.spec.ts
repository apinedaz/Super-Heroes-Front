import { UppercaseNameDirective } from './uppercase-name.directive';

describe('UppercaseNameDirective', () => {
  it('should create an instance', () => {
    const directive = new UppercaseNameDirective();
    expect(directive).toBeTruthy();
  });
});
